# Arrays
# Slice (copy) an array
my_array = your_array[:]

# Return the length of an array
len(my_array)

# Check if an array is empty
if not len(my_array):

# Return the largest number:
x = max(5, 10)